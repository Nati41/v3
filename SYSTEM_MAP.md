# מפת מערכת - Tofesly (v3)

> מסמך זה מתאר את מבנה הפרויקט כולו.
> נכתב למפתח חדש שנכנס לפרויקט בלי שום רקע.

---

## מהו הפרויקט?

**Tofesly** (תפס.לי) היא פלטפורמה למיפוי ומילוי טפסי PDF בעברית.

**הרעיון המרכזי:**
1. **מיפוי** - משתמש מקצועי (רואה חשבון, יועץ מס) פותח טופס PDF ומסמן בדיוק איפה כל שדה נמצא: שם פרטי כאן, ת.ז. כאן, חתימה כאן
2. **שמירה** - המיפוי נשמר כתבנית JSON שאפשר לעשות בה שימוש חוזר
3. **מילוי** - נתוני לקוח (מאקסל, ידני, או AI) נכנסים לתבנית ומייצרים PDF מלא מוכן להגשה

**קהל יעד:** רואי חשבון, יועצי מס, עורכי דין, יועצי משכנתאות
**טכנולוגיה:** JavaScript וניילה (ללא פריימוורק), ארכיטקטורה מודולרית, עובד 100% בצד הלקוח (ללא שרת backend)

---

## 6 הכלים הגדולים

### 1. Mapper v3 - כלי המיפוי (`/src/mapper-v3/`)
> הלב של המערכת. כאן מתבצע המיפוי החזותי.

- משתמש טוען PDF
- מסמן שדות על ידי ציור מלבנים
- המערכת מזהה אוטומטית גבולות שדות (AutoBoxer)
- כל שדה מקבל שם עברי ושם קנוני (canonical) באנגלית
- תומך בשדות טקסט, צ'קבוקס, רדיו, טבלאות וחתימות
- פועל ב-3 מצבים: **מיפוי מונחה** (Guided), **מיפוי חופשי**, **מצב ניסיוני** (Reverse)

### 2. Fill Engine - מנוע המילוי (`/src/fill-engine/`)
> המנוע שמכניס נתונים לתוך ה-PDF.

- מקבל תבנית JSON (מהמיפוי) + נתוני לקוח
- מייצר PDF מלא עם כל השדות ממולאים
- תומך: טקסט (עברית RTL), צ'קבוקס, רדיו, טבלאות, חתימות
- התאמה סמנטית חכמה - מחבר בין שם שדה לנתון גם אם הניסוח שונה
- אינטגרציה עם אקסל - קורא ומפרש נתונים מקבצי Excel/CSV

### 3. LiveFill - מילוי ללקוח (`/src/livefill/`)
> ממשק פשוט שהלקוח רואה.

- הלקוח מקבל טופס PDF עם שדות שכבר מופו
- ממלא את מה שחסר (הפרטים שלו)
- רואה תצוגה מקדימה חיה על גבי ה-PDF
- מייצא PDF סופי

### 4. Mobile Fill - מילוי מנייד (`/src/mobile-fill/`)
> אפליקציה מותאמת לטלפון.

- ממשק מגע מותאם לנייד (iOS/Android)
- תמיכה במצלמה + OCR (זיהוי טקסט מתמונה)
- ניווט שדה-אחר-שדה מותאם למסך קטן
- קטלוג טפסים מוכנים

### 5. Core - תשתית משותפת (`/src/core/`)
> שכבת התשתית שכולם משתמשים בה.

- **UnifiedStateManager** - ניהול מצב מרכזי (שדות, ערכים, היסטוריה)
- **UnifiedEventBus** - מערכת אירועים (תקשורת בין מודולים)
- **PDFService** - טעינה ורינדור של PDF
- **CoordinateService** - המרת קואורדינטות בין מערכות צירים

### 6. Shared - כלים משותפים (`/src/shared/`)
> ספריית עזר שכל המודולים צורכים.

- `fieldSchema.js` - ולידציה של מבנה שדות
- `UnifiedCoordinateSystem.js` - מערכת קואורדינטות אחידה
- `FieldIntentResolver.js` - החלטה איך לרנדר שדה (תאים / טקסט חופשי)
- `tables/` - מודל נתונים וממשק לטבלאות

---

## המנועים המרכזיים

> המנועים יושבים בתוך `/src/mapper-v3/engines/` ומבצעים את העבודה הכבדה.

### AutoBoxer (`AutoBoxer.js`)
**מנוע פיזיקה לזיהוי שדות**
- עובד על פיקסלים בלבד (לא מנתח מבנה PDF)
- המשתמש לוחץ בנקודה → המנוע "שולח קרניים" לכל הכיוונים
- מחפש רצפה (קו תחתון), קירות (שוליים), תקרה (גבול עליון)
- מחזיר מלבן מדויק שמגדיר את גבולות השדה
- מכויל במיוחד לטפסים ממשלתיים בעברית

### BboxRefiner (`BboxRefiner.js`)
**שיפור הדרגתי של מלבנים**
- אחרי שה-AutoBoxer מציע מלבן ראשוני, ה-Refiner משפר אותו
- מייצר מועמדים לכל צלע (שמאל, ימין, למעלה, למטה)
- לחיצה בחוץ → הרחבה, לחיצה בפנים → כיווץ, גרירה → כיוונון עדין
- מונע התנגשות עם שדות וטקסט קיימים

### DrawController (`DrawController.js`)
**ניהול תהליך הציור**
- מתרגם אירועי עכבר (mousedown/move/up) לפעולות מיפוי
- משלב בין AutoBoxer ו-BboxRefiner
- תומך במצבי ציור שונים: שדה טקסט, צ'קבוקס, רדיו, טבלה, חתימה
- מזהה כוונת משתמש (intent) דרך IntentManager

### OverlayRenderer (`OverlayRenderer.js`)
**רינדור שכבת שדות חזותית**
- מציג מלבנים צבעוניים מעל ה-PDF
- כל שדה שמופה מקבל אוברליי ויזואלי
- תומך בגרירה, שינוי גודל, בחירה
- מנהל שכבת "רוחות" (ghost overlays) לשדות תבנית שעדיין לא מופו

### PDFEngine (`PDFEngine.js`)
**רינדור PDF**
- טוען PDF בעזרת ספריית pdf.js
- מרנדר ב-300 DPI ל-Canvas ואז ממיר ל-PNG
- מציג כתמונה (לא canvas) לשיפור ביצועים
- שומר cache לכל דף למעבר מהיר בין דפים

### TextExtractor (`TextExtractor.js`)
**חילוץ טקסט מה-PDF**
- מוציא טקסט מתוך שכבת הטקסט של ה-PDF (pdf.js)
- חלופה: OCR דרך Tesseract.js (לטפסים סרוקים)
- מחזיר רק טקסט שנמצא לגמרי בתוך המלבן שהמשתמש ציירים

---

## מי משתמש במי - תרשים תלויות

```
                    ┌──────────────────────────────────────────┐
                    │            משתמש קצה (דפדפן)              │
                    └────┬──────────┬──────────┬───────────────┘
                         │          │          │
                    ┌────▼───┐ ┌────▼───┐ ┌────▼────────┐
                    │Mapper  │ │LiveFill│ │Mobile Fill  │
                    │  v3    │ │        │ │             │
                    └──┬─────┘ └──┬─────┘ └──┬──────────┘
                       │          │          │
              ┌────────┼──────────┼──────────┘
              │        │          │
         ┌────▼────────▼──┐  ┌───▼───────────┐
         │  Fill Engine   │  │   Core         │
         │  (מנוע מילוי)   │  │ (State, Events,│
         │                │  │  PDF, Coords)  │
         └────┬───────────┘  └───┬────────────┘
              │                  │
              └──────┬───────────┘
                     │
              ┌──────▼──────────┐
              │    Shared       │
              │ (schemas, utils,│
              │  coordinates,   │
              │  tables)        │
              └─────────────────┘
```

**תלויות ישירות:**
| מודול | תלוי ב... |
|-------|-----------|
| Mapper v3 | Core, Shared, Fill Engine (לייצוא) |
| LiveFill | Core, Shared, Fill Engine |
| Mobile Fill | Shared (חלקית), pdf.js |
| Fill Engine | Shared (schemas, semantic) |
| Core | Shared (coordinates, schemas) |

---

## מערכות קואורדינטות (קריטי להבנה!)

> אחד הדברים הכי מורכבים בפרויקט. ארבע מערכות צירים שונות.

### 1. PDF פיזי (PDF Points)
- יחידות: נקודות PDF (72 נקודות = אינץ')
- ציר Y: **מלמטה למעלה** (0 בתחתית הדף)
- שימוש: מבנה ה-PDF הפנימי

### 2. נורמלי (Normalized)
- יחידות: 0 עד 1 (יחסי לגודל הדף)
- ציר Y: **מלמטה למעלה** (כמו PDF)
- שימוש: שמירה ב-JSON (בלתי תלוי ברזולוציה)

### 3. Canvas/Render
- יחידות: פיקסלים (300 DPI)
- ציר Y: **מלמעלה למטה** (כמו מסך)
- שימוש: רינדור ה-PDF ועבודת ה-AutoBoxer

### 4. Overlay/DOM
- יחידות: פיקסלים CSS
- ציר Y: **מלמעלה למטה** (כמו מסך)
- שימוש: מיקום מלבנים ויזואליים מעל ה-PDF

**נוסחת המרת Y:**
```
y_screen = (1 - y_normalized - height_normalized) * layerHeight
```

> כל באג בקואורדינטות כמעט תמיד נובע מבלבול בין מערכות הצירים.

---

## מצבי עבודה

### מצב מיפוי (Mapping Mode)
**איפה:** Mapper v3
**מה קורה:** המשתמש מסמן שדות על PDF ובונה תבנית
**מי פעיל:**
- `DrawController` - ציור מלבנים
- `AutoBoxer` + `BboxRefiner` - זיהוי גבולות
- `OverlayRenderer` - תצוגת שדות
- `TextExtractor` - חילוץ שמות שדות
- `StateManager` - שמירת מצב
- `TemplateStore` - ניהול תבנית AI (אופציונלי)
- `GuidedMappingUI` - ממשק מיפוי מונחה (אופציונלי)

### מצב מילוי (Fill Mode)
**איפה:** LiveFill / Mobile Fill
**מה קורה:** הלקוח ממלא שדות וממיר ל-PDF
**מי פעיל:**
- `Fill Engine` - מילוי בפועל
- `SemanticMatcher` - התאמה חכמה בין נתונים לשדות
- `font-manager` - ניהול פונטים (עברית)
- `ExcelDataResolver` - קריאת נתוני אקסל (אופציונלי)

### מצב ניסיוני (Reverse Mapping)
**איפה:** Mapper v3 → Reverse Mapping
**מה קורה:** המשתמש מצייר אלמנטים פיזיים, AI מזהה מה הם
**מי פעיל:**
- `ReverseMappingMode` - בקר ראשי
- `DrawController` - ציור (עם חיתוך מוקדם ל-REVERSE_DRAW_COMPLETE)
- `AutoBoxer` + `BboxRefiner` - זיהוי גבולות
- `ReverseMappingOverlay` - תגיות [מספר | סוג]
- `ReverseMappingAI` - זיהוי סמנטי

### מצב AI
**איפה:** Mapper v3 → AI Service
**מה קורה:** AI (Claude) מנתח את הטופס ומציע מיפוי אוטומטי
**מי פעיל:**
- `AIService` - תקשורת עם Claude API
- `FieldIntelligenceStore` - שמירת תוצאות AI
- `TemplateStore` - ניהול תבנית AI
- `template-analyzer.js` - פרומפטים לניתוח תבנית
- `field-intelligence-prompt.js` - פרומפטים לזיהוי שדות

### מצב מובייל
**איפה:** Mobile Fill
**מה קורה:** משתמש ממלא טופס מהטלפון
**מי פעיל:**
- `mobile-flow-controller` - ניהול זרימה
- `mobile-pdf-viewer` - תצוגת PDF מותאמת
- `mobile-input-panel` - ממשק קלט מגע
- `mobile-ocr-service` - זיהוי טקסט ממצלמה
- `validation-engine` - ולידציית שדות

---

## ארכיטקטורה מרכזית

### Event-Driven (מונחה אירועים)
כל התקשורת בין מודולים עוברת דרך `EventBus`. מודול אחד "פולט" אירוע, אחרים "מאזינים" לו.

**דוגמה:** כשמשתמש מסיים לצייר מלבן:
```
DrawController → emit('DRAW_END') → StateManager שומר → OverlayRenderer מצייר → Sidebar מתעדכן
```

**50+ סוגי אירועים** מכסים: שדות, UI, תבניות, טבלאות, רדיו, AI, ייצוא

### State Management (ניהול מצב)
- **StateManager** (`mapper-v3/core/`) - מצב המיפוי (שדות, קבוצות, טבלאות)
- **TemplateStore** - מעקב אחרי תבנית AI (שדות שמופו/לא מופו)
- **FieldIntelligenceStore** - נתוני AI סמנטיים (שם קנוני, הקשר, פורמט)
- **IntentManager** - כוונת ציור נוכחית (מה המשתמש מנסה לעשות)

### Singleton Pattern
רוב המודולים מייצאים **singleton** - מופע יחיד גלובלי:
```javascript
export const autoBoxer = new AutoBoxer();
export const state = new StateManager();
export const eventBus = new EventBus();
```

---

## מבנה תיקיות - מפה מהירה

```
v3-main/
├── SYSTEM_MAP.md          ← אתה כאן
├── server.js              ← שרת פיתוח (Node.js, אוטו-פורט 3000+)
├── dev.html               ← פורטל פיתוח (כל הכלים)
├── package.json           ← תצורת פרויקט (v3.13.0)
│
├── src/                   ← קוד מקור פעיל
│   ├── core/              ← תשתית משותפת (State, Events, PDF, Coords)
│   ├── mapper-v3/         ← כלי המיפוי הראשי
│   │   ├── core/          ← ליבת מיפוי (State, Events, Templates)
│   │   ├── engines/       ← מנועי עיבוד (AutoBoxer, Draw, Overlay...)
│   │   ├── ai/            ← אינטגרציית AI (Claude)
│   │   ├── ui/            ← ממשק משתמש (דיאלוגים, מסכים)
│   │   ├── reverse-mapping/ ← מצב ניסיוני
│   │   ├── pre-mapper/    ← עיבוד מקדים (ייבוא, זיהוי טבלאות)
│   │   ├── overlay/       ← שכבת תוויות
│   │   ├── helpers/       ← עוזרים (שמות קנוניים)
│   │   ├── tables/        ← ניהול טבלאות (stub)
│   │   └── styles/        ← CSS
│   ├── fill-engine/       ← מנוע מילוי PDF
│   │   └── semantic/      ← התאמה סמנטית חכמה
│   ├── livefill/          ← מילוי ללקוח (דסקטופ)
│   ├── mobile-fill/       ← מילוי מנייד
│   └── shared/            ← כלים משותפים
│       └── tables/        ← מודל טבלאות
│
├── assets/                ← משאבים סטטיים
│   ├── fonts/             ← פונטים (David Libre, Noto Sans)
│   ├── forms/             ← טפסי PDF לדוגמה (101, 106...)
│   ├── mappings/          ← תבניות מיפוי מוכנות
│   └── css/               ← סגנונות גלובליים
│
└── _legacy/               ← ארכיון גרסאות ישנות (לא בשימוש)
    ├── mapper/            ← Mapper v1
    └── mapper-v2/         ← Mapper v2
```

---

## מונחים חשובים

| מונח | הסבר |
|------|------|
| **bbox** | Bounding Box - מלבן גבולות שמגדיר מיקום שדה (x, y, width, height) |
| **canonical** | שם קנוני באנגלית לשדה (למשל `first_name`, `id_number`) |
| **overlay** | שכבה שקופה שמוצגת מעל ה-PDF עם מלבנים ויזואליים |
| **template** | תבנית JSON שמתארת את כל השדות בטופס ומיקומם |
| **AutoBoxer** | מנוע פיזיקה שמזהה גבולות שדה מנקודת לחיצה |
| **BboxRefiner** | שיפור הדרגתי של ה-bbox אחרי הזיהוי הראשוני |
| **normalized** | קואורדינטות יחסיות (0-1) שלא תלויות ברזולוציה |
| **RTL** | Right-To-Left - כתיבה מימין לשמאל (עברית) |
| **DPI** | Dots Per Inch - צפיפות רינדור (72=PDF, 300=תצוגה) |
| **ghost overlay** | מלבן "רוח" שמציין שדה בתבנית שעוד לא מופה |
| **intent** | כוונת משתמש - מה הפעולה הבאה שהוא רוצה לבצע |
| **semantic** | סמנטי - נתונים על **משמעות** השדה (לא מיקום פיזי) |
| **singleton** | דפוס תכנות - מופע יחיד של מחלקה בכל המערכת |
| **EventBus** | "אוטובוס אירועים" - מערכת שמאפשרת למודולים לתקשר בלי להכיר אחד את השני |

---

*מסמך זה נוצר כתיעוד לפרויקט. אין לשנות קבצי קוד - תיעוד בלבד.*
